#include <stdio.h>

int main(){
    float y; //deklarasi var. float y, karena merupakan bil. pecahan

    //menampilkan pernyataan (1) y dan (2) y
    printf("(1) y = b*x*x + 0.5*x\n"); 
    printf("(2) y = (0.3*x*z) / (2*a)");
    return 0;
}